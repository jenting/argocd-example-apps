apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../guestbook

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: guestbook-ui
      spec:
        replicas: 2
